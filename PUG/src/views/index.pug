html
    head
        link(rel='stylesheet', type='text/css', href='../style.css')
        title PUG
    body
        h2 Listado de productos
        
        a(href= "/")| Ir a cargar un nuevo producto 
        div(class='products')
            each producto in prod.length ? prod : ['NO SE ENCONTRARON PRODUCTOS']
                div(class="articulos")
                    h3= producto.title
                    h3= producto.price
                    img(src= producto.thumbnail)